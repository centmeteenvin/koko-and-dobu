class: Dorm
table: dorm
fields:
    lat: double
    long: double
    websiteUrl: String?
    members: List<User>?, relation(name=dorm_members)
    owner: User?, relation
    outgoingRequests: List<DormJoinRequest>?, relation(name=dorm_join_request_dorm)

indexes:
    dorm_owner_unique_idx:
        fields: ownerId
        unique: true